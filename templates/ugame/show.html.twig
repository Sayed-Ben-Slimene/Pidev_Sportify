{% extends 'adminBase.html.twig' %}

{% block title %}Game
{% endblock %}

{% block body %}
	<h1>Game</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ game.id }}</td>
			</tr>
			<tr>
				<th>DateTime</th>
				<td>{{ game.dateTime ? game.dateTime|date('Y-m-d H:i:s') : '' }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_ugame_index') }}">back to list</a>

	<a href="{{ path('app_ugame_edit', {'id': game.id}) }}">edit</a>

	{{ include('ugame/_delete_form.html.twig') }}
{% endblock %}
