{% extends 'Admin.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ commentaire.comment }}</td>
            </tr>
            <tr>
                <th>DateHeur</th>
                <td>{{ commentaire.dateHeur ? commentaire.dateHeur|date('Y-m-d : H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ commentaire.user }}</td>
            </tr>
            <tr>
                <th>NbLike</th>
                <td>{{ commentaire.nbLike }}
            </tr>
            <tr>
                <th>NbDislike</th>
                <td>{{ commentaire.nbDislike }}

            </tr>
            <tr>
                <th>Actualite</th>
                <td>{{commentaire.blog.titre}}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_edit', {'id': commentaire.id}) }}">edit</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
